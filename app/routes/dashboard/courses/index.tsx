import { Link } from "@remix-run/react";
import { primaryButtonClasses } from "~/utils/classes";

export default function CoursesIndexPage() {
	return (
		<div className="my-5">
			Click on a course to view it's content!
		</div>
	)
}